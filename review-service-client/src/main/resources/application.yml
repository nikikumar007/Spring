spring:
  application:
    name: review-service-client
    

server:
  port: 5858
  
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka
